set ts=2 sw=2                   " Default to 2 space tabs.
set expandtab                   " Always expand tabs into spaces.
set autoindent                  " Autoindent by default
set ruler                       " Show the ruler at the bottom of the screen.
set incsearch                   " Show search as it is typed.
set hlsearch                    " Highlight all found terms. Use :nohl to hide.

nnoremap <F5> :buffers<CR>:buffer<Space>
nmap <F1> <nop>  " Disable the F1 key, which I accidentally press all the time.
